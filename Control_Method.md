# 現状のクルーズコントロール制御手法 (PID + FF)

現在の制御は、車速に対するPI制御とフィードフォワード(FF)制御を組み合わせた構成となっており、目標車速を維持するためにアクセルペダルの開度（シミュレーション値0〜100%、実際は上限30%）を計算しています。

## 1. 制御入力と前処理
- **目標車速 (`targetSpeed`)** と **現在車速 (`currentSpeed`)** の差分（エラー）に基づいて制御を行います。
- 現在車速にはローパスフィルタ (指数移動平均: EMA、係数 `SPEED_FILTER_ALPHA = 0.05`) をかけて平滑化 (`smoothedSpeed`) しています。

## 2. フィードフォワード制御 (ベースライン)
- 基準となるベースペダル開度 (`BASELINE_PEDAL_PERCENT`) は **8.0%** に設定されています。
- **目標速度付近での減衰 (動的ベースライン):**
  - 車速エラーが `BASE_DECAY_THRESHOLD_KPH` (8.0 km/h) を下回り、かつプラス（目標速度に達していない）場合、ベース開度はエラーに比例して減衰します。
  - `current_base = BASELINE_PEDAL_PERCENT * (error / 8.0)`
  - **エラーが0以下（目標速度に到達、または超過）になった瞬間に `current_base = 0.0%` (アクセル全閉) となります。**

## 3. PID制御
現在すべてのギア(1速〜6速)で同じパラメータが使用されています。
- **P（比例）ゲイン `Kp`:** 1.2
- **I（積分）ゲイン `Ki`:** 0.13
- **D（微分）ゲイン `Kd`:** 0.0

- **P項:** `Kp * error`
- **I項:** `Ki * integral`
  - 積分値（I項）は最大 `INTEGRAL_MAX_PERCENT` (15.0%) に制限（アンチワインドアップ対策）されています。
- **D項:** 現在は `Kd` が0のため機能していません。

### チューニング
 - 現在のデフォルト値（参考）
   - Kp (通常Pゲイン): 0.8 (目標±5km/h以内の応答)
   -  Kp_High (遠距離Pゲイン): 3.0 (目標±5km/h以上の強い加速)
   - Ki (Iゲイン): 0.5 (定常偏差・坂道の補正)
   - Kd (Dゲイン): 0.5 (変化のブレーキ・安定化)
1. 症状別の調整ガイド
   - 症状①：目標速度に到達するまで時間がかかる（加速がたるい・モタつく）
     - 原因: 比例（P）または積分（I）の効きが弱い。
     - 調整方向:
       - Kp（または Kp_High）を上げる (+0.2 〜 +0.5)
       - 速度差があるときに速やかにアクセルを踏み込むようになります。
       - Ki を少し上げる (+0.1 〜 +0.2)
         - 目標速度に近づいた最後の「あと1〜2km/h」がなかなか届かない場合に効果的です。
   - 症状②：速度が波打つ・ペダルが小刻みにバタつく（ハンチング）
     - 原因: Pゲインが高すぎる、またはDゲインが強すぎて振動している。
     - 調整方向:
       - Kp を下げる (-0.2 〜 -0.4) （まずはこれから試す）
       - 速度の変化に対する過剰反応を抑えます。
       - Kd を調整する
         - ガクガクした細かい振動なら Kd を下げる (-0.1 〜 -0.2)
         - ゆっくり大きくゆらゆら波打つなら Kd を少し上げる (+0.1 〜 +0.2) （ダンピング効果）
   - 症状③：設定速度を超えて大きく行き過ぎてから戻る（オーバーシュート）
     - 原因: 加速中の「勢い（慣性）」が行き過ぎる、またはI項の蓄積が残っている。
     - 調整方向:
       - Kd を上げる (+0.1 〜 +0.3)
       - 車速が目標に急接近したときに「ブレーキ（アクセルを戻す方向）」を早めにかけて行き過ぎを防ぎます。
       - Ki を少し下げる (-0.1 〜 -0.2)
       - 加速中に溜まりすぎるI項を抑えます。
   - 症状④：上り坂に入ったとき、速度が落ち込んでから遅れて加速する
     - 原因: 坂による抵抗に対してI項（過去の蓄積）の立ち上がりが遅い。
     - 調整方向:
       - Ki を上げる (+0.1 〜 +0.2)
       - 速度低下を検出したときに、ベースラインを持ち上げる力を早く溜めます。
   - 症状⑤：下り坂を抜けた直後、急にガクッと加速する / 減速が長引く
     - 原因: エンジンブレーキ無効化（3%下限）に伴い、下り坂でP項がマイナスに振れすぎている。
     - 調整方向:
       - Kp を少し下げる (-0.1 〜 -0.2)
       - ※今回は下り坂での「目標超過時 integral = 0 清算」を導入しているため、基本的には下り坂明けの挙動は安定しているはずですが、戻りが急な場合はP項を少しマイルドにします。
1. おすすめの調整手順（実走時）
   - まずは Kp（Pゲイン）の反応速度をチェック
     - 平坦路でセットした際、不快な振動（ハンチング）が出ず、素直に加速する範囲に Kp を合わせます。
   - 次に Ki（Iゲイン）で定常速度を維持
     - 目標速度ぴったりで車速がピタッと安定するように Ki を調整します（小さすぎると目標-1〜2km/hで止まり、大きすぎると波打ちます）。
   - 最後に Kd（Dゲイン）で収束性を微調整
     - 加速完了時の「行き過ぎ（オーバーシュート）」や「収束の収まりの良さ」を Kd で整えます。

## 4. 出力計算と制限
- **出力の合算:** `output = current_base + pTerm + iTerm + dTerm`
- **変化率制限 (レートリミッター):** 1周期(20ms想定※Config.hによる)あたりの出力変動は `OUTPUT_MAX_CHANGE_PER_CYCLE` (5.0%) 以内に制限され、急激なアクセル操作を防いでいます。
- **絶対値制限:** 最終的なペダル出力は **0.0% 〜 30.0%** の範囲にクリップされます。

## 5. 人間操作との協調 (バックキャルキュレーション)
- ドライバーがアクセルを操作した際に、その開度に合わせて積分値(`integral`)を逆算(`syncIntegral`関数)し、制御復帰時のショック（段差）をなくす工夫がされています。

---

## 車速パルス計測フィルターロジック

車速はJIS D 1601準拠の車輪速パルス（4パルス/回転）をISR（割込みサービスルーチン）で取得し、`updatePulse()` 内で多段フィルタを通して `pulseSpeed` [km/h] に変換しています。ノイズ（髭パルス）やパルス欠落による誤った車速値がギア推定を狂わせ、制御キャンセルに至ることを防ぐ設計です。

### 処理フロー全体像

```
ISR (speedPulseISR)
│  RISINGエッジ割込みで発火
│  latestPulseTime_s ← micros()
│  latestPulseInterval_s ← 今回 - 前回の時刻差
│  newPulseOccurred_s ← true
│
▼
updatePulse() ← 毎ループ呼び出し
│
├── [新パルスあり？] ── No ──→ タイムアウト判定へ
│       │
│      Yes
│       │
│   ① 初回パルス判定
│   │  lastValidPulseTime == 0 → 基準時刻を記録して終了
│   │
│   ② 200km/h 上限カット
│   │  パルス間隔 ≦ 7000μs (≒201.8km/h) → 無条件棄却
│   │
│   ③ 突発ヒゲ棄却フィルタ（10km/h以上走行中のみ）
│   │  前回有効間隔の 85%未満に急激短縮 → 棄却（高速側ノイズ）
│   │  前回有効間隔の 3倍超に急激伸長  → 棄却（パルス欠落）
│   │
│   ④ 車速変化量フィルタ（最終防衛線）★新規追加
│   │  新車速と現在pulseSpeedの差が 5kph以上 → 棄却
│   │  （初回 or 停止復帰時は pulseSpeed==0 のためスキップ）
│   │
│   ⑤ 有効パルス → pulseSpeed 更新
│      pulseSpeed = 1412873 / パルス間隔[μs]
│      lastValidInterval, pulsePeriodUs, lastValidPulseTime を更新
│
└── タイムアウト判定
    最終パルスから 2秒経過 → 停車とみなしリセット
    pulseSpeed=0, lastValidInterval=0, lastValidPulseTime=0
    ISR側の共有変数もクリア
```

### 各フィルタ段の詳細

#### ① 初回パルス判定
停止状態（`lastValidPulseTime == 0`）から最初のパルスが来た場合、間隔を計算する基準点がないため、時刻だけ記録して車速計算はスキップします。2発目のパルスから車速計算が始まります。

#### ② 200km/h 上限カット
パルス間隔が 7000μs 以下（≒201.8km/h 超）のパルスは物理的にあり得ないため、無条件で棄却します。ノイズが極端に短い間隔で発生した場合の最初のガードです。

#### ③ 突発ヒゲ棄却フィルタ
10km/h以上の走行中（`lastValidInterval < 141287μs`）にのみ有効です。

| 条件                              | 判定 | 想定シナリオ                       |
| --------------------------------- | ---- | ---------------------------------- |
| `diff < lastValidInterval × 85%`  | 棄却 | 髭ノイズで見かけ上パルスが詰まった |
| `diff > lastValidInterval × 300%` | 棄却 | パルス欠落で見かけ上間隔が伸びた   |

85%閾値は、正常な加速による間隔短縮と、ノイズによる異常な短縮を区別するためのものです。通常の加速では1パルス間隔で15%以上短縮することはほぼありません。

#### ④ 車速変化量フィルタ（最終防衛線）★ロバスト化アップデート
③のパルス間隔レベルのフィルタをすり抜けたノイズに対する最終防衛線です。算出した新車速（`newSpeed`）と現在の `pulseSpeed` を比較し、**5kph以上の急変** があればノイズとして棄却します。

デッドロック（誤検出された車速で固まってしまう現象）を防ぐため、以下の2つのセーフティを導入してロバスト化しています：
1. **適用条件（30kph制限）**: `pulseSpeed >= 30.0f`（クルーズコントロール制御が動作する中高速域）でのみ適用。30kph未満の低速・発進・停止時には車速変化量が大きくても棄却しません。
2. **連続一致による強制復帰**: 30kph以上で一時的な急変として棄却された場合でも、新車速と一時変数（`tempLastNewSpeed`）の差が連続3回にわたり 5kph 以内に収まった（新しい車速帯で安定した）場合は、「ノイズではなく実際の急激な車速変化」とみなして車速更新を再開・許可します。

- **閾値**: `PULSE_SPEED_MAX_DELTA = 5.0f` [kph]
- **連続一致回数**: 3回 (`consecutiveValidCount >= 3` で更新許可)

#### ⑤ タイムアウト処理
最後のパルスから **2秒間** 新しいパルスが来なければ停車と判定し、全ての車速関連変数をリセットします。リセット後にパルスが再び来れば①から自動的に復帰します。

### ギア推定への影響
`estimateGear()` は `pulseSpeed` と `currentRPM` の比からギア段を推定します。上記フィルタにより `pulseSpeed` の異常値が排除されるため、ノイズによるギア誤判定（`currentGear == 0`）が防止され、制御中の不要なキャンセルを回避できます。

---

## 課題（MT車での目標車速付近でのギクシャク感の原因）
1. **ベースラインの急激なゼロ化:**
   車速が目標車速をわずかでも上回った(エラー ≦ 0)瞬間に、FF項のベースラインが **0%** になります。さらにP項もマイナスまたはゼロになるため、アクセル出力が急激に0に張り付きます。
2. **MT車特有の強いエンジンブレーキ:**
   アクセルが0%になるとAT車に比べて強いエンジンブレーキがかかるため、車速がすぐに落ち込みます。
3. **ハンチング（オンオフ動作）の発生:**
   車速が落ち込んでエラーがプラスになると再びアクセル（ベースライン＋P項）が立ち上がり、また目標速度を超えるとアクセル0%…というループが発生し、これが「オンオフされるような気持ち悪い挙動」の原因となっています。

---

## 6. 課題解決のための追加ロジック（ソフト・デッドバンド ＋ I項の動的FFホールド）
上記の「ギクシャク感（ハンチング）」を解決し、さらに坂道などの環境変化にも自動で適応するため、以下の追加ロジックが実装されています。

### A. ベースライン(FF)の連続化
目標車速を超過した場合でも、ベースライン（現在は8.0%固定）を強制的に0%に落とす処理を廃止しました。超過分の減速は、P項とI項がマイナスに働くことによって滑らかに行われます（最終出力の段階で下限0%でクリップ）。これにより、急激なエンジンブレーキを防ぎ、滑らかな減速感を実現します。

### B. ソフト・デッドバンド（不感帯）
目標車速の **±0.3km/h** の範囲を「不感帯（デッドバンド）」として設定しています（※以前の±1.0km/hから、よりこまめに速度を維持するために狭めました）。
単に境界線でエラーをゼロにすると出力に段差（ショック）が生じるため、実際のエラーから0.3km/hを引いた「有効エラー」をPI制御に用いる **「ソフト・デッドバンド」** 方式を採用しています。
- **デッドバンド外:** 目標に向けて滑らかに加速・減速（PIDが機能）
- **デッドバンド境界:** 有効エラーが0になり、P項が段差なく滑らかに0に収束

### C. I項の学習と動的FFホールド機能
車速がデッドバンド（±1.0km/h）に入り有効エラーが `0` になると、**I項（積分値）の更新がストップ（ホールド）** されます。
坂道や向かい風など、平地とは異なる環境で走行している際、I項はその環境の「ズレ」を埋めるために自然に蓄積（学習）されます。そしてデッドバンドに入った瞬間にその学習値がホールドされるため、以後は `ベースライン(8.0%) + 学習済みI項` が **環境に適応した動的なフィードフォワード（FF）補正値** として機能し、アクセル開度をピタッと安定させます。

### D. I項の蓄積制限（アンチ・ワインドアップ制御）
目標車速に向かって加速・減速している最中にI項が過剰に蓄積し、目標到達時にアクセルが抜けなくなる「積分ワインドアップ（オーバーシュートの原因）」を防ぐためのロジックです。

**【以前のロジックと問題点（※現在は廃止）】**
以前は「目標車速から **±5.0km/h** 以上離れている時は、I項の新規蓄積を行わず、P項とベースラインの出力だけで車速をコントロールする」という速度差ベースの制限を行っていました。しかし、速度差が大きい状態でP項＋ベースラインの出力が上限に満たない（例：23%程度）状態になると、車速が目標に近づかず、いつまでもI項が解禁されない「デッドロック状態（踏み込まない状態）」に陥る問題が発生しました。

**【現在のロジック（標準的な出力クランプ方式）】**
上記の問題を解消するため、一般的なアンチワインドアップ（出力張り付き時の積分停止）に変更しました。
PWM出力が上限（30%）または下限（0%）に達しており、かつエラーがその張り付きをさらに強める方向にある場合のみ、I項の蓄積を停止します。出力が限界に達していない間は常にI項が蓄積されるため、デッドロックに陥ることなく目標速度へ確実に近づけるようになります。また、I項自体の最大値も `INTEGRAL_MAX_PERCENT` (15.0%) に制限し、過剰な蓄積に対する二重の対策としています。

### G. 車速超過時のI項非対称ゲイン＆上限の動的引き下げ（新規追加）
車速が目標を大幅に上回った際、過去に蓄積されたI項（積分値）が残り続けることによってアクセルが抜け切らず、車速が戻らない課題を解消するため、以下の非対称制御を追加しました。
1. **非対称積分ゲイン（`Ki_Decay`）の適用**
   - 通常走行（車速不足時）の積分ゲイン `Ki` は `0.13` ですが、車速が目標を超過している（エラーがマイナス）のときは、減少用積分ゲイン `Ki_Decay` として `0.30` を適用します。これにより、超過時にはI項が素早く減少します。
   - **【改善点】** 以前は出力時にゲインを掛けていたため、不感帯をまたいでゲインが切り替わった瞬間にI項がステップ状に跳ね上がる（急変する）という課題がありました。現在は、**エラーを積分（蓄積）する時点でゲインを掛け合わせる**方式に変更したため、過去の蓄積値が保護され、ゲイン切り替え時も出力が連続的かつ滑らかに変化するようになっています。
2. **I項クランプ上限の動的引き下げ**
   - 車速の超過量に応じて、蓄積できるプラス方向のI項の上限（通常 `15.0%`）を動的に制限します。
   - 超過が `+1.0 kph` から `+4.0 kph` に向かうにつれて、I項の上限値を `15.0%` から `0.0%` へ線形に引き下げます。
   - 超過が `+4.0 kph` を超えた場合は上限が完全に `0.0%` になり、プラス方向のI項は強制的に消去され、アクセルを余計に踏み続ける要因を徹底的に排除します。
   - これにより、下り坂などで急激にアクセルを全閉（0%）にしてエンジンブレーキの強いショックを発生させることなく、滑らかにアクセルを抜いて車速を目標に戻します。

### I. エンジンブレーキ完全無効化（最小PWM 3.0%固定）とオーバーシュート対策【最新仕様】
下り坂等でのアクセル0%化に伴う強いエンジンブレーキとアクセルONのバタつき（ギクシャク感）をなくすため、エンジンブレーキを一切使用しない制御に変更されています。

- **現在の仕様 (エンジンブレーキ無効化 & 最小3.0%固定):**
  - **最小PWM出力の固定:** 車速超過時も含め、PWM下限値を常に **3.0%** に固定します（スロットル全閉・0%出力を廃止）。
  - **オーバーシュート（車速超過）対策:** 車速が目標値を超えている間 (`raw_error < 0`)、過去に蓄積された加速用の正の積分値 (`integral > 0`) を即座に **0% にリセット・クリア** します。これにより、エンジンブレーキが効かない下り坂を通過した直後でも、過去のI項によってアクセルが余分に残り再加速してしまう現象を防ぎます。

- **【旧仕様・現在は非適用】動的PWM下限制御（エンジンブレーキ自動調整）:**
  > ※以前は、通常時は下限3.0%としつつ、目標車速を1.0km/h以上超過した際に超過量に応じて下限を3.0%から0.0%へ引き下げ、2.0km/h以上の超過で完全0%（スロットル全閉で強烈なエンジンブレーキ発動）にするロジックを適用していました。しかし下り坂でアクセルON/OFFが繰り返され不快感が生じるため、現在は廃止（非適用）となっています。

### H. 折れ線ゲイン制御（遠距離時の加速力向上）
アンチ・ワインドアップ制御により遠距離からの加速がP項のみに依存することになったため、加速力不足を補うためのロジックです。
エラー（速度差）の大きさに応じて、Pゲインを2段階に切り替えます。
* **近距離（±5.0km/h以内）:** 標準のゲイン（`Kp = 1.2`）を適用し、滑らかで繊細な制御を行います。
* **遠距離（±5.0km/h以上）:** 強いゲイン（`Kp_High = 3.0` など）を適用し、力強く加速します。

境界点（±5.0km/h）において出力が急変（ガクつき）しないよう、数式上で「連続的につながる折れ線グラフ」として計算しています。これにより、MAXの加速力を得つつも目標に近づくにつれてシームレスにペダルが抜けていく快適な乗り心地を実現しています。

### F. 半クラ時ギアシフト検出による早期制御復帰

**【以前のロジックと問題点（※現在は廃止）】**
以前は「クラッチスイッチが離された（Released判定）」瞬間にPID制御を復帰させていました。しかし実際のMT車ではクラッチスイッチが離れる前に半クラ状態で既にギアが繋がり始めており、その間は制御出力が0%のままとなるため「もたつき」として体感される問題がありました。

**【現在のロジック（ギアシフト検出方式）】**
1. **クラッチPress時のギアを記録：** クラッチスイッチが押された瞬間のギアを `gearAtClutchPress` として記録します。
2. **上位ギア検出で即出力開始：** クラッチを踏んでいる間もギア推定（RPM/車速比）は動作し続けます。RPMが低下して上位ギアと判定された場合（`currentGear > gearAtClutchPress`）、半クラ状態でギアが噛み始めていると判断し、クラッチスイッチがリリースされる前でもPID制御出力を開始します。
3. **Gear:0でのクラッチリリースは制御キャンセル：** クラッチを離した時にギアが0（ニュートラル判定）の場合、安全のため制御を即座にキャンセルします（ブレーキを踏んだのと同じ扱い、Target車速は保持）。これにより、ニュートラルのままクラッチを繋いで惰性走行に入った場合に吹け上がることを防止します。
